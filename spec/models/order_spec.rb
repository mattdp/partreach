#every order has a user
#quantity exists
#quantity is above 0
#can't hit id via mass assignment
#destroying user destroys order (request)
#can only see one's own orders (request)
#admin can see all orders (request)