<% provide(:title, "Metrics") %>

<table id="rfq-table">
	<tr>
		<% @titles.each do |title| %>
			<th><%= "#{title}" %></th>
		<% end %>
	</tr>
	<% @printout.each do |unit| %>
		<tr>
			<% unit.each do |datum| %>
			<td><%= "#{datum}" %></td>
			<% end %>
		</tr>
	<% end %>
</table>

<%= render 'layouts/analytics_links' %>