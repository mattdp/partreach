<% provide(:title, "Metrics") %>

<div class="container">
  <div class="row">
  	<div class="col-sm-10 col-sm-offset-1 main-area-contrast override">

			<p>Note that all event-based metrics are valid from the metric's creation date forward.</p>

			<table id="rfq-table">
				<tr>
					<% @titles.each do |title| %>
						<th><%= "#{title}" %></th>
					<% end %>
				</tr>
				<% @printout.each do |unit| %>
					<tr>
						<% unit.each do |datum| %>
						<td><%= "#{datum}" %></td>
						<% end %>
					</tr>
				<% end %>
			</table>

			<%= render 'analytics/links' %>

		</div>
	</div>
</div>