<% provide(:title, "New lead creation") %>

<div class="container">
  <div class="row">
  	<div class="col-sm-10 col-sm-offset-1 main-area-contrast override">

  		<%= render partial: "leads/lead_form_content", locals: {} %>

  		<%= link_to "Back to leads index", leads_path %>

  	</div>
  </div>
</div>