<% provide(:title,"Testimonials from SupplyBetter users") %>
<% meta_description "Many businesses have shared how much SupplyBetter helped them" %>

<div class="container">
  <div class="row">
    <div class="col-sm-10 col-sm-offset-1 main-area-contrast override">

    <h3 class="bit-of-space center">Buyer testimonials</h3>
    <p class="center">We make life better for buyers. Here's what some of them have to say.</p>

    <table>
      <% @testimonials.each do |testimonial| %>
        <tr>
          <td>
            <%= "<a href='#{testimonial[:site_url]}'>".html_safe if testimonial[:site_url].present? %>
            <%= image_tag testimonial[:logo] %>
            <%= "</a>".html_safe if testimonial[:site_url].present? %>
          </td>
          <td class="padding-left">
            <p class="left"><%= "#{testimonial[:praise]}" %></p>
            <p class="right">
              <%= "-#{testimonial[:person]}, #{testimonial[:title]}, "%>
              <% if testimonial[:site_url].present? %>
                <%= link_to "#{testimonial[:company]}", testimonial[:site_url] %>
              <% else %>
                <%= "#{testimonial[:company]}" %>
              <% end %>
            </p>
          </td>
        </tr>
      <% end %>
    </table>

    <p class="center bit-of-space"><%= link_to "Get Started", order_questions_path, class: "btn btn-xl supplybetter-blue center" %></p>

    <p class="space-above center">Or head <%= link_to 'back to home', root_path %>.</p>

    </div>
  </div>
</div>