<% if communications and communications != [] %>
	<% communications.each do |c| %>
		<%= "#{c.created_at.strftime('%Y-%m-%d')}: #{c.means_of_interaction}/#{c.interaction_title} - #{c.notes}" %>
	<% end %>
<% end %>