<%= form_tag action: action, controller: "tags" do %>
	
	<%= label_tag :name %>
	<%= text_field_tag :name, tag.name %>

	<%= label_tag :readable %>
	<%= text_field_tag :readable, tag.readable %>

	<%= label_tag :family %>
	<%= text_field_tag :family, tag.family %>

	<%= label_tag :note %>
	<%= text_area_tag :note, tag.note %>

	<%= label_tag :visible, tag.visible %>

	<%= label_tag :exclusive, tag.exclusive %>

	<%= submit_tag "Submit", class: "save-button" %>

<% end %>