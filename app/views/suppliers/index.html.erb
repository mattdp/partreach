<% provide(:title,"Supplier Profiles") %>

<div class="content order">
	<div class="container row supplier-list">s
		<p id="supplier-list-intro">Supplier profiles are still being developed - this is a small sample for testing. We have 250+ suppliers catalogued, representing 20+ countries.</p>

		<% @visibles.keys.each do |country| %>
			<h3 class="supplier-list-country"><%= "#{country}" %></h3>
			<% @visibles[country].keys.each do |state| %>
				<h4 class="supplier-list-state"><%= "#{state}" unless state == "no_state" %></h4>
				<% @visibles[country][state].each do |s| %>
					<ul class="supplier-list-bullets">
						<li><%= link_to s.name, "/suppliers/#{s.name_for_link}" %></li>
					</ul>
				<% end %>
			<% end %> 
		<% end %>
	</div>
</div>