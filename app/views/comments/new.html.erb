<% provide(:title, "Add a comment") %>

<% heading = "Adding #{@verbose_type} for '#{@provider.name}'" %>

<%= render partial: 'comments/comments_form', locals: {heading: heading, path: comments_path, method: :post} %>
