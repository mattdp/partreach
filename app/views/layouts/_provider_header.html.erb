<header class="header">
  <div class="header__inner">
    <h1 class="header__logo">
      <%= link_to teams_index_path do %>
        <%= image_tag "supplybetter-logo-white-cropped.svg", :alt => "SupplyBetter" %>
      <% end %>
    </h1>
    <ul class="header__links">
      <% if signed_in? %>
        <% if current_user.supplier_id %>
          <li><%= link_to "Edit my supplier page", edit_supplier_path(current_user.supplier_id) %></li>
        <% end %>
        <li class="dropdown">
          <a href="#" class="dropdown-toggle" data-toggle="dropdown"><%= "Questions? Need help?" %></a>
          <ul class="dropdown-menu">
            <li><a href="mailto:matt@supplybetter.com">Email the founders directly</a></li>
            <li><a>Chat: We're here! Bottom of the page!</a></li>
            <li><a>Redphone support: 415-238-2438</a></li>
          </ul>
        </li>
        <li class="dropdown">
          <a href="#" class="dropdown-toggle" data-toggle="dropdown"><%= "#{current_user.lead.lead_contact.email}" %></a>
          <ul class="dropdown-menu">
            <li><%= link_to "Sign out", signout_path, method: "delete" %></li>
          </ul>
        </li>
      <% end %>
    </ul>
  </div>
</header>
